<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head', { pageTitle: 'Dodge calculator' }) %>
  </head>
  <body>
    <%- include('partials/header', { navActive: 'calculator' }) %>

    <main class="page-main page-main--wide">
      <div class="page-intro">
        <h1>Dodge calculator</h1>
        <p>Pick your team and the enemy comp—we'll compare average win rates.</p>
      </div>

      <%- include('partials/championDataNotice') %>

      <form class="card card--form" action="/results" method="post">
        <div class="team-picker">
          <div class="team-column team-column--ally">
            <h2>Ally team</h2>
            <% const allySlots = [
              { id: 'top', name: 'top', label: 'Top' },
              { id: 'jungler', name: 'jungler', label: 'Jungler' },
              { id: 'mid', name: 'mid', label: 'Mid' },
              { id: 'bot', name: 'bot', label: 'Bot' },
              { id: 'support', name: 'support', label: 'Support' },
            ]; %>
            <% allySlots.forEach((slot) => { %>
            <%- include('partials/championAutocompleteField', slot) %>
            <% }) %>
          </div>

          <div class="team-column team-column--enemy">
            <h2>Enemy team</h2>
            <% const enemySlots = [
              { id: 'etop', name: 'etop', label: 'Top' },
              { id: 'ejungler', name: 'ejungler', label: 'Jungler' },
              { id: 'emid', name: 'emid', label: 'Mid' },
              { id: 'ebot', name: 'ebot', label: 'Bot' },
              { id: 'esupport', name: 'esupport', label: 'Support' },
            ]; %>
            <% enemySlots.forEach((slot) => { %>
            <%- include('partials/championAutocompleteField', slot) %>
            <% }) %>
          </div>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn">Calculate</button>
          <p>Not sure how to play your champion? <a href="/championPage">Find a video guide</a></p>
          <div class="form-footer-links">
            <a href="/" class="btn btn--secondary">Back to home</a>
          </div>
        </div>
      </form>
    </main>

    <%- include('partials/footer') %>
    <%- include('partials/championAutocompleteSetup') %>
  </body>
</html>
